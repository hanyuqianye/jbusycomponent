#summary How to setup an environment to build this project with maven and eclipse.
#labels Featured,Phase-Implementation

= Software installation =

  * install eclipse: http://www.eclipse.org/downloads/
  * install subeclipse (svn): http://subclipse.tigris.org/
  * install sonatype naven-eclipse plugin: http://m2eclipse.codehaus.org/
  * Optional: install maven command line tool: http://maven.apache.org/download.html

= JBusyComponent download =
  * For maven user, just add a section to your pom dependencies section, for example, to get 1.0.0 release
{{{
<dependency>
    <groupId>com.google.code</groupId>
    <artifactId>jbusycomponent</artifactId>
    <version>1.0.0</version>
</dependency> 
}}}
  * For manual download, look at http://repo1.maven.org/maven2/com/google/code/jbusycomponent/

= Configuration =
Due to dependencies (jxlayer, swingx) which are not deploy into the central repository (or not te right version), you have to configure maven to add another repository:

Create or update your ${user.home}/.m2/settings.xml
{{{
<settings xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                      http://maven.apache.org/xsd/settings-1.0.0.xsd">
	<profiles>
		<profile>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<repositories>
				<repository>
					<id>javanet</id>
					<name>Java.net Repository for Maven 2</name>
					<releases>
						<enabled>true</enabled>
						<updatePolicy>never</updatePolicy>
						<checksumPolicy>fail</checksumPolicy>
					</releases>
					<snapshots>
						<enabled>false</enabled>
					</snapshots>
					<url>http://download.java.net/maven/2/</url>
					<layout>default</layout>
				</repository>
			</repositories>
		</profile>
	</profiles>
</settings>
}}}


= Usage =

  * If you already have the project into your workspace, just active it:
     Right click on project item > Maven > Enabled Dependency Management
  * import from scratch:
     File > New > Other
      Select Maven > Checkout Maven Project From SCM ... and enter project information.

= Don't panic =

first import / activation is much slower. All is fine, it's because maven need to initialize its local repository and it download some dependency.


======== incoming ============

JBusyComponent is deployed into Sonatype OSS repository http://oss.sonatype.org/
This respository is naturally synced with the Central Maven repository.


=== Configuring Maven for deploying on Sonatype OSS repository ===

You muse edit you file %USER_HOME%/.m2/settings.xml : 
